import styled from "styled-components";

export const Container = styled.div`
  background: var(--primary);

`;

export const Wrapper = styled.div`
  height: 100%;
  width: 1200px;
  margin: 0px auto;

  display: flex;
  justify-content: center; // centralizando todos itens do nosso layout;



`;